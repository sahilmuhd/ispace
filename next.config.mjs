/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Populate with the real CDN/host once production photography is supplied.
    remotePatterns: []
  }
};

export default nextConfig;
